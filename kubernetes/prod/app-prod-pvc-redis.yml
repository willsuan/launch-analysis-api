apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: artemis-prod-redis-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: cinder-csi
  resources:
    requests:
      storage: 1Gi
