apiVersion: v1
kind: Service
metadata:
  name: artemis-test-api-nodeport
spec:
  type: NodePort
  selector:
    app: artemis-test-api
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30500
